# Spec-Driven LLM Wiki - Kilocode

Use `AGENTS.md` as the canonical workflow contract.

This repo stores self-contained spec-driven development memory:

- `spec/` for numbered specs and handoffs.
- `wiki/` for modular project knowledge.
- `graph/` for relationship data and HTML visualization.
- `tools/` for TypeScript automation.

Run the same workflow triggers documented in `AGENTS.md`.
